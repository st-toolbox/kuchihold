# kuchihold ― 口腔運動ミラートレーニング支援アプリ 企画提案

## 1. 背景と課題

言語聴覚療法（ST）では、構音障害・嚥下障害・顔面神経麻痺などに対して
**口腔・顔面の運動訓練**を行います。現場には次の課題があります。

- **どこまで動かせばよいか患者に伝わりにくい。** 「もっと大きく」と言葉で
  伝えても、目標量が患者の感覚と一致しない。
- **ミラーセラピーの効果。** 鏡で自分の運動を見ながら行うと運動学習が
  促進されるとされるが、卓上の鏡では口元が小さく、角度や距離も安定しない。
- **自分の動きはブレて見えにくい。** 顔や体が動くと像が揺れ、口元の細かな
  変化を観察しづらい。
- **自主練習の質が担保しにくい。** ST がいない場面で、何を目標にどれだけ
  行ったかが残らない。

## 2. ソリューション概要

スマホ／タブレットのインカメラで自分を撮影し、**口元だけを切り出して
拡大・安定表示**するミラートレーニングアプリ。映像の上に**目標**（手書き／
スタンプ／目標の口形＝シャドウ／開口度の目標値）を重ね、患者は「自分の顔に
描かれた目標」に合わせて運動する。指導者（ST）が目標を設定するモードと、
患者が一人で練習するモードを用意し、**患者ごと（管理番号）に記録**する。

### 設計の核：顔ロック座標系

本アプリの肝は「顔ロック座標系」です。口元クロップ枠を**顔の位置・大きさ
（・任意で傾き）にロック**して平滑化（EMA）する一方、**唇の開閉そのものは
ロックしない**。これにより、

- 顔や体が動いても表示は**ブレない**（＝口元が常に画面中央に安定して大写し）、
- それでいて**口の動きはしっかり見える**、
- 重ねた目標（手書き・スタンプ・シャドウ）は同じ座標系なので**自動的に顔へ
  追従**し、「顔に書き込まれている」ように見える、

という、課題に直結した体験を同時に満たします。

## 3. 主要機能

| 機能 | 内容 | 対応する課題 |
| --- | --- | --- |
| 口元の安定拡大表示 | 顔ロック座標系＋平滑化で口元のみを大写し。鏡表示。 | ブレ／ミラーセラピー |
| 手書き・スタンプ目標 | 画面に目印（線・円・▲など）を描き、顔に追従させる | 目標量の可視化 |
| シャドウ目標 | 目標到達時の口形を撮影し半透明で重ね、次回の目標に | 目標量の可視化 |
| 縦・横メーターと目標値 | 「あ（縦の開き）」と「い（横の広がり）」を別々に数値化し、目標ラインと到達判定を表示 | 「どこまで」を定量化 |
| 到達回数カウント | 設定した目標（縦・横の両方）に達するたびに自動カウント | 自主練習の質 |
| 2モード | ST指導（編集可）／自主練習（編集ロック） | 指導と自習の両立 |
| 患者ごとの記録 | 管理番号で識別。目標プリセットと練習ログを保存 | 記録・継続支援 |

## 4. 画面とモード

- **ST指導モード**：目標の描画・スタンプ・シャドウ撮影・開口度目標の設定、
  映像安定化（鏡／傾き補正／ズーム／ブレ抑制）の調整、目標プリセットの保存。
- **自主練習モード**：編集はロック。割り当てた目標を読み込み、開口度メーター・
  到達回数・セッション記録に集中。UI を簡潔化。

左：患者（管理番号）と保存済み目標・記録／中央：口元ステージ／右：モード別操作。

## 5. 技術構成（プロトタイプ）

- **フロントエンド**：React 18 + htm（ビルド不要構成。将来 Vite + TypeScript へ移行可）
- **顔・口唇検出**：MediaPipe Tasks Vision `FaceLandmarker`（ブラウザ内・端末で完結、
  サーバへ映像を送らない）
- **描画**：Canvas 2D（顔ロック変換・オーバーレイ・シャドウ合成）
- **保存**：localStorage（管理番号ごとのプリセット・セッションログ）

> 本リポジトリには上記を実装した**動くプロトタイプ**が含まれます（`index.html`,
> `src/`）。npm が使えない環境でも動くよう CDN + import map 構成にしています。
> 実行手順は `README.md` を参照してください。

## 6. プライバシー・安全・規制上の留意点

- **個人情報を持たない設計**：氏名は扱わず管理番号のみ。映像はブラウザ内処理で
  サーバ送信しない。
- **保存データ**：プロトタイプは端末の localStorage。実運用では端末内暗号化・
  バックアップ・端末紛失時の扱い・院内規程との整合を要検討。
- **医療機器該当性**：診断や治療効果を標榜する場合は薬機法上のプログラム医療
  機器（SaMD）該当性の確認が必要。本アプリは「訓練の補助・記録ツール」と位置づけ、
  診断・治療効果の標榜は行わない方針を推奨。
- **アクセシビリティ**：高齢者・麻痺のある利用者を想定し、大きな表示・単純な操作・
  音や色のフィードバックを継続的に改善する。

## 7. ロードマップ案

1. **MVP（本プロトタイプ）**：口元安定表示・手書き/スタンプ・シャドウ・開口度目標・
   記録・2モード。
2. **計測の高度化**：口角の左右差、上下唇の対称性、左右口角の挙上量など、訓練種目別の
   定量指標と達成度グラフ。
3. **種目テンプレート**：構音（/pa//ta//ka/ 等）・嚥下・顔面麻痺リハの定番メニューを
   プリセット化。
4. **記録の共有**：ST と患者間での目標配布・実施状況の同期（オプトイン）。
5. **TypeScript 化と PWA 対応**：Vite + TS へ移行、オフライン起動・ホーム画面追加。

## 8. 既知の制約（プロトタイプ）

- カメラ利用には `https` または `http://localhost`（セキュアコンテキスト）が必要。
- MediaPipe のモデル/WASM は初回にネットワーク取得（端末のブラウザから）。
- 指標は基本的に顔サイズで正規化した相対値。実寸（mm）は片目幅32mmを仮定した
  概算（推定値）で、個人差・顔の向き・レンズ歪みで誤差がある。正確な実寸には
  深度カメラ等が必要。
- 傾き補正は既定オフ（顔が大きく傾く場面向けの補助機能）。

---

ご確認のうえ、優先したい機能や臨床現場での制約（対象疾患、利用端末、院内ルール等）が
あれば反映します。次段階として Vite + TypeScript 化や種目テンプレートの作り込みも可能です。
